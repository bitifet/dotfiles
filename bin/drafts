#!/usr/bin/env bash

# Installation:
#     - Place this script in a directory in your path
#     - chmod +x ...
#
# Usage:
#     - cd $(drafts)
# 
# Alternative usage (if you prefer...):
#     - pushd $(drafts)
#     - ...
#     - popd
#
# Utility:
#     - Pretty obvious... 😉

DRAFTS_BASE="${HOME}/Drafts"
TODAY=$(date +%Y%m%d);
YYYY=${TODAY:0:4};
MM=${TODAY:4:2};
DRAFTS_TODAY="${DRAFTS_BASE}/${YYYY}/${MM}/${TODAY}";
mkdir -p ${DRAFTS_TODAY}
echo $DRAFTS_TODAY;
