#!/bin/bash

grep -REc "$1" * \
    | grep -v ':0$' \
    | perl -pe 's/:.*//' \
    | grep -Ev '(^|/)\.svn($|/)' \
    | grep -Ev '(^|/)\.sass-cache($|/)' \
    | grep -Ev '.sw[pon]$' \
    | grep -Ev '(^|/)node_modules/'
