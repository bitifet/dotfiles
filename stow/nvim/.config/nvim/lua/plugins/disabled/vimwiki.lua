return {
    {
        "vimwiki/vimwiki",
        lazy = false, -- Ensure it loads immediately
        -- config = function()
        --     vim.g.vimwiki_list = {
        --         {
        --             path = vim.fn.expand("~/vimwiki/"),
        --             syntax = 'markdown', -- Use Markdown syntax
        --             ext = '.md', -- Use Markdown file extension
        --             global_ext = 0, -- Disable global extension
        --         }
        --     }
        -- end,
    },
}
